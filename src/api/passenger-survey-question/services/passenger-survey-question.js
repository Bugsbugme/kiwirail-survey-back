'use strict';

/**
 * passenger-survey-question service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::passenger-survey-question.passenger-survey-question');
