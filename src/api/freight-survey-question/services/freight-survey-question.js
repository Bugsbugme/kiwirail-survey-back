'use strict';

/**
 * freight-survey-question service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::freight-survey-question.freight-survey-question');
